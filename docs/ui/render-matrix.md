# Render Matrix

updated: 2026-08-20

| Surface | Viewport | Content / state | Input | Build | Render | Interaction | Comfort | Narrative restraint |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Concept probes | default desktop | Identical synthetic DIR-01 | browser snapshot | n/a | passed | n/a | Docket selected | passed; probe banner prevents result confusion |
| Production docket | 1440 × 900 | DIR-01 exact evidence and blocked proposal; CLN-01/02, SEM-01, XART-03 transitions | mouse + Enter on Run | passed | passed; capture `evidence/review-docket-desktop.png` | passed; stable seals and counts | passed; fixed split remains readable | passed; fixture provenance and rejection consequence only |
| Production docket | 390 × 844 | DIR-01 with long IDs and evidence | touch-sized controls + keyboard-compatible native controls | passed | passed; capture `evidence/review-docket-narrow.png` | passed; table scroll preserves column semantics | passed with horizontal docket scroll | passed |
| Saved actual result | 1440 × 900 | Vertex DIR-02 plus transitions through SEM-01, XART-03, CLN-01, CLN-02 | mouse/select | passed | passed; capture `evidence/review-docket-actual-vertex.png` | passed; actual provider/model label and raw seals matched | passed | passed; actual and fixture provenance remain distinct |
| Private Cloud Run docket | browser default | Revision `ecr-poc-00002-v9g`; Vertex DIR-02, XART-03, CLN-01, CLN-02 plus fixture rejected span | mouse/select through authenticated proxy | passed in Cloud Build | passed; capture `evidence/review-docket-cloud-run.png` | passed; actual counts/seals, hidden rejected evidence, and 403 boundary verified | passed | passed; frozen Vertex and fixture provenance remain distinct |
| V2 published-result repair | browser default 1280 × 720 | Local published v1 compatibility plus fixture rejection; GCS-unavailable error is API-tested | mouse/select/candidate activation | passed locally | passed in fresh in-app browser review | published/fixture selection passed; rejected evidence remained hidden | passed at default viewport; prior narrow contract unchanged, fresh narrow override unavailable | passed; provenance stays in existing environment/footer roles |
| Final private Cloud v2 | 1440 × 900 and 390 × 844 | Revision `ecr-poc-00005-485`; run `cloud-v2-20260820T035505Z-56ad91df`; DIR-01, SEM-02, XART-04, CLN-01, BEN-02 | mouse/select, Enter candidate activation, responsive measurement | passed in Cloud Build | passed in actual deployed Chrome; console warning/error 0 | counts, seals and Top-6 match raw API; one `aria-pressed`; 43.59 px source target; desk live region | passed; only docket owns 345/680 px horizontal overflow at 390 px | passed; full run/v2 freeze/commit provenance without instructional prose |
| Private Cloud v3 audit | 1440 × 900 and 390 × 844 | Revision `ecr-poc-00006-6jz`; cold start then run `cloud-v3-20260820T043842Z-6e260831`; rapid Published/Fixture switching | mouse/select, keyboard Enter, responsive measurement | passed in Cloud Build | failed overall; cold-start wording passed but Reload wrapped at 390 px | failed; stale response overwrote newer selection and focus fell to BODY after Reload | failed; accidental two-line primary control at 390 px | passed; loading text is state-only and prior instructional-copy finding is closed |
| Final private Cloud v4 | 1440 × 900 and 390 × 844 | Revision `ecr-poc-00007-xvc`; cold start, 28 rapid transitions, run `cloud-v4-20260820T050914Z-92f72d97`; DIR-01, SEM-02, XART-04, CLN-01, BEN-02 | mouse/select, keyboard Enter, CUA horizontal scroll | passed in Cloud Build | passed in actual deployed browser; console warning/error 0 | latest request always owns state; Reload focus restored; API counts/order/seals match; rejected evidence hidden | passed; Reload 126 × 38 one line, body overflow 0, table-local 345/680 px scroll | passed; state/provenance/fail-closed copy only |
| Local v5 active contract | 1440 × 900 and 390 × 844 | DIR-01 verified + blocked, XART-03 two targets, first catalog 503/retry, published fixture snapshot, published-unavailable error/recovery, changed source/original/changed content | mouse, touch-sized controls, keyboard Enter/Reload/ArrowLeft/ArrowRight, 20 rapid source/case transitions | passed locally | passed in actual in-app browser after independent audit repair; captures `evidence/review-docket-v5-desktop.jpg` and `evidence/review-docket-v5-narrow.jpg` | latest request owns XART-03 state; result failure clears rows/evidence/provenance; catalog retry restores 18 cases/6 rows without duplication; all withheld records are selectable; ArrowRight moves table scroll 0→320 px | passed; body overflow 0, Reload 126 px nowrap, change comparison collapses to one column, table-local 360/680 px overflow | passed; source authority, mutation data, state, provenance, evidence, and recovery copy only |
| Final private Cloud v5 | browser default and 390 × 844 | Revision `ecr-poc-00008-pk2`; published run `cloud-v5-20260820T072055Z-3efe8584`; DIR-01, XART-03, XART-04 withheld records, 20 rapid case transitions | mouse/select, withheld-record activation, keyboard Home/ArrowRight | passed in Cloud Build | passed in actual in-app browser through authenticated proxy | latest request settled on XART-03; XART-04 showed 1 verified/5 blocked and every withheld record; evidence remained hidden; published provenance matched commit/manifest/fingerprint | passed; body overflow 0, table-local 345/680 px scroll moved 0→320 | passed; fixture and published authority, provenance, and fail-closed consequences remain explicit |
| Local v6 Live Review · purpose-driven contract | 1440 × 900 and 390 × 844 | 4,170-character input, validation/index recovery, `NO_SUPPORTED_REVIEW`, fixture `REVIEW_REQUIRED`, fixture `INCONCLUSIVE`, Broad/Expanded/Final scope, ten candidate/claim rows | mouse plus semantic keyboard/focus checks | passed locally | passed in fresh in-app browser | result focused `result-summary`; validation/index failure focused `form-error` and preserved input; supported claim showed exact lines/evidence; inconclusive showed none; one pressed candidate | passed; body overflow 0; narrow docket 317/760 px local scroll; billable action 330 × 38 one line | passed; persistent copy is purpose, state, non-obvious limitation, cost, or provenance |
| Local v6 Evaluation · frozen regression | 1440 × 900 and 390 × 844 | 20 cases, Final Top-10 fixture result, Incoming Artifact summary, atomic verified/blocked DIR-01, five rapid case transitions ending XART-03 | mouse/select and focusable docket | passed locally | passed in fresh in-app browser | latest request settled on XART-03 with 10 rows/one pressed candidate; supported exact claim visible; blocked text hidden; fixture/benchmark authority explicit; ArrowRight moved 0→143 px | passed; body overflow 0; table-local 827/1012 desktop and 345/1012 narrow overflow; Reload 38 px one line | passed; benchmark is labeled observed/frozen and never unseen performance evidence |

Browser evidence: CLN-02 showed 0 verified/0 blocked; SEM-01 showed 1/0; XART-03 showed 2/0; DIR-01 showed 1/1. Selecting the rejected DIR-01 candidate displayed `No evidence exposed.` and only the deterministic blocking stage.

After the actual run, saved-result mode showed DIR-02 4/0, SEM-01 1/0, XART-03 2/0, CLN-01 0/0, and CLN-02 2/0 (verified/blocked). The last row is an actual control false alarm, not a UI fixture.

The deployed revision showed `SAVED EVALUATION · vertex-adk · gemini-3.5-flash` and matched DIR-02 4/0, XART-03 2/0, CLN-01 0/0, and CLN-02 2/0. Its fixture rejection exposed no evidence and identified `deterministic_exact_span` as the blocking stage.

Explanation-deletion rule: the persistent footer keeps only the non-obvious fixture provenance constraint. Visible labels identify objects, state, verification, or rejection consequences; no paragraph narrates the page structure.

The v2 repair review confirmed that legacy v1 case UUIDs are not presented as the published run identity: the environment and footer use the published-result metadata run ID (`c31aabbe-91a…`). The available in-app browser ignored its temporary 390 × 844 viewport override, so fresh narrow rendering is explicitly unverified; the previously captured narrow contract remains the current evidence.

The final deployed v2 re-audit supersedes that viewport limitation: 1440 × 900 and 390 × 844 both rendered the published run `cloud-v2-20260820T035505Z-56ad91df` with commit `10c59bf`. The full run ID is available in the accessible environment title and footer, the compact visible form keeps its timestamp and suffix, and the footer names `ecr-poc-preregistered-v2`. Body-wide overflow was absent; horizontal scrolling was confined to the docket table.

The deployed v3 audit closes the v2 loading-copy exception: cold start displays `Loading review disposition…` while `Reload result` is disabled. It also found three major defects requiring a later freeze: an overlapping-request stale response, a two-line Reload control at 390 px, and lost keyboard focus after Reload. Representative result values and provenance still matched the published API when requests were allowed to settle.

The final v4 re-audit closes all three v3 majors. Across 28 rapid source/case transitions, every control, case identity, title, provenance field, count, and seal belonged to the latest request. At 390 × 844, Reload remained 126 × 38 px with one line and no body overflow; the docket alone scrolled horizontally. Enter Reload restored focus and the 2 px amber ring after completion, while source/case initiated loads retained native select focus.

The local v5 extension keeps the same Docket grammar and adds the required mutation provenance before the candidate table. Published fixture snapshots are labeled separately from live fixture mode and explicitly remain non-LLM evidence; an actual published Vertex v5 state remains unverified until the approved billable run and publish path execute. The explanation-deletion test removed operating instructions: result source, case, changed artifact comparison, candidate dispositions, evidence gates, and contextual error text were sufficient to complete the task.

The independent v5 audit initially failed four major gates: stale result/context mixing, inaccessible duplicate/off-Top-K blocked records, no narrow keyboard path to hidden comparison columns, and no initial catalog retry. Repair verification confirmed result failure with zero candidate rows and no evidence/provenance, an inspectable `deterministic_exact_span` record with hidden evidence, named table focus with deterministic keyboard scroll, and fixture recovery. A one-shot local proxy then returned `simulated transient catalog outage`; the real UI showed the server detail and `Retry catalog`, and retry restored exactly 18 cases and six DIR-01 rows without duplicate options or listeners.

The final v5 Cloud review closes the approval-gated evidence boundary. The environment banner and footer identify the actual Vertex provider, baseline run, source commit, manifest, and full accessible embedding fingerprint. XART-04 demonstrated the live fail-closed path: five schema-missing decisions were selectable, each showed `No evidence exposed`, and only the supported DESIGN_RUN_LOOP review was visible.
